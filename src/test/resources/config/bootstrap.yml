spring:
  application:
    name: jhipsterSampleApplication
  cloud:
    config:
      enabled: false
    compatibility-verifier:
      enabled: false
    gateway:
      discovery:
        locator:
          enabled: false
eureka:
  client:
    enabled: false
